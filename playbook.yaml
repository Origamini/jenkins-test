---
- name: Insall Apache on ubuntu
  hosts: worker
  become: yes
  tasks:
    - name: Update cache
      apt:
        update_cache: yes
    - name: Install apache
      apt:
        name: apache2
        state: present
    - name: start apache service
      service:
        name: apache2
        state: started
